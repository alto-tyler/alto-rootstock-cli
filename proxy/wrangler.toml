name = "alto-rootstock-auth"
main = "worker.js"
compatibility_date = "2024-11-01"

[vars]
ALLOWED_DOMAIN = "altoconsultants.ca"
GITHUB_REPO = "alto-tyler/alto-rootstock-skills"
GITHUB_REF = "main"

# Secrets (do NOT put these here — set with `wrangler secret put <NAME>`):
#   GOOGLE_CLIENT_ID
#   GOOGLE_CLIENT_SECRET
#   GITHUB_TOKEN
#   SESSION_SECRET
